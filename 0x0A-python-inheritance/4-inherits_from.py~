#!/usr/bin/python3
"""Is sub class module"""


def inherits_from(obj, a_class):
    """Returns true if an object is an instance
       subclass, otherwise false"""
    return (issubclass(type(obj), a_class) && (type(obj) is not a_class))
